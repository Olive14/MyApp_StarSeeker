<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="starseeker.com.myapplication">

    <!-- インターネットに接続するためのパーミッション 2017/06/19追記 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- GPSベースで現在位置を取得するためのパーミッション 2017/06/20追記 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- ネットワークベースで現在位置を取得するためのパーミッション 2017/06/20追記 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launch_img"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StarSeekerMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>